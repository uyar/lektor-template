<!DOCTYPE html>
<html lang="${ this.alt }">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <meta name="description" content="${ bag('site', this.alt, 'description') }"/>
  <meta name="keywords" content="${ bag('site', this.alt, 'keywords') }"/>
  <title tal:content="this.title + ' — ' + site.env.project.name">Page title</title>
  <link rel="shortcut icon" href="${ '/static/favicon.svg' >> url }" type="image/svg"/>
  <link rel="stylesheet" href="${ '/static/main.css' >> url }"/>
</head>
<body>
  <header>
    <nav aria-label="main navigation">
      <ul>
        <li>
          <a href="${ '/' >> url(alt=this.alt) }" tal:content="bag('translations', this.alt, 'home')">Home</a>
        </li>
        <li tal:repeat="menu_item [site.get('/' + slug, alt=this.alt)
                                   for slug in bag('site', 'menus', 'main').split()]"
            tal:attributes="class 'active' if this.is_child_of(menu_item) else None">
          <a href="${ menu_item >> url }" tal:content="menu_item.title">Item title</a>
        </li>
      </ul>
    </nav>

    <nav class="alternatives">
      <ul>
        <li tal:repeat="lang get_alts(this)" tal:attributes="class 'active' if lang == this.alt else None">
          <a href="${ '.' >> url(alt=lang) }" tal:content="bag('translations', lang, '_name')">Language</a>
        </li>
      </ul>
    </nav>
  </header>

  <main metal:define-slot="content">
    Page content
  </main>

  <footer>
    <small tal:content="bag('site', this.alt, 'license')">Copyright and license</small>
  </footer>
</body>
</html>
